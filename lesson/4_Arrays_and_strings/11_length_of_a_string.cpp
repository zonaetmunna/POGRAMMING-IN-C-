#include <iostream>
#include <string>

int main() {
    std::string str = "Hello, World!";
    int length = str.length();

    std::cout << "Length of the string: " << length << std::endl;

    return 0;
}
